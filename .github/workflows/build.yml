name: 打包刘辩心纸君桌宠

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-windows-exe:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install PyQt5 pyinstaller

    # 修复编码和路径打包逻辑：加了 --collect-all PyQt5 并显式用 powershell 执行
    - name: Build EXE with PyInstaller
      shell: powershell
      run: |
        $env:PYTHONUTF8=1
        pyinstaller --noconsole --onefile --add-data "*.png;." --add-data "*.jpg;." --name "LiuBianPet" main.py

    - name: Upload Executable
      uses: actions/upload-artifact@v4
      with:
        name: LiuBianPet-Windows
        path: dist/LiuBianPet.exe
